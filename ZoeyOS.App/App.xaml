<Application x:Class="ZoeyOS.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>

            <!-- === Palette === -->
            <Color x:Key="BgDeepColor">#0B0E14</Color>
            <Color x:Key="BgPanelColor">#12161F</Color>
            <Color x:Key="BgCardColor">#171C28</Color>
            <Color x:Key="TextPrimaryColor">#EAEEF5</Color>
            <Color x:Key="TextMutedColor">#A3ABBD</Color>
            <Color x:Key="AccentCyanColor">#4FD8E8</Color>
            <Color x:Key="AccentVioletColor">#8C6FF0</Color>
            <Color x:Key="AccentAmberColor">#F0B65C</Color>
            <Color x:Key="AccentGreenColor">#5BE0A0</Color>
            <Color x:Key="IdleGlowColor">#3A4356</Color>

            <SolidColorBrush x:Key="BgDeepBrush" Color="{StaticResource BgDeepColor}"/>
            <SolidColorBrush x:Key="BgPanelBrush" Color="{StaticResource BgPanelColor}"/>
            <SolidColorBrush x:Key="BgCardBrush" Color="{StaticResource BgCardColor}"/>
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
            <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>
            <SolidColorBrush x:Key="AccentCyanBrush" Color="{StaticResource AccentCyanColor}"/>
            <SolidColorBrush x:Key="AccentVioletBrush" Color="{StaticResource AccentVioletColor}"/>
            <SolidColorBrush x:Key="AccentAmberBrush" Color="{StaticResource AccentAmberColor}"/>
            <SolidColorBrush x:Key="AccentGreenBrush" Color="{StaticResource AccentGreenColor}"/>
            <SolidColorBrush x:Key="IdleGlowBrush" Color="{StaticResource IdleGlowColor}"/>

            <!-- === Base text style === -->
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
            </Style>

            <Style x:Key="MutedText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>

            <!-- === Scrollbar (dark) === -->
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Width" Value="8"/>
            </Style>

            <!-- === ComboBox (dark closed box, white dropdown with black text) ===
                 No style existed for ComboBox before, so every dropdown (Model picker,
                 Tool access picker) was rendering with raw default WPF chrome: the closed
                 box didn't pick up this app's dark theme at all, and the default toggle
                 button glyph looked like a separate raised 3D button. This is an implicit
                 style (no x:Key, TargetType only) so it applies to every ComboBox in the
                 app automatically - nothing else needs to change per-dropdown.

                 IMPORTANT: setting Foreground="Black" on ComboBoxItem itself is NOT enough.
                 Each item's visible text is an auto-generated TextBlock nested inside the
                 ContentPresenter below, and this app's app-wide implicit
                 "Style TargetType=TextBlock" (near-white, for the dark theme everywhere
                 else) overrides that inherited color on every TextBlock, including this
                 one - a Style setter on a descendant always beats an inherited value from
                 an ancestor. The actual fix is scoping a LOCAL implicit TextBlock style
                 to just this Border's subtree (via Border.Resources): WPF's implicit-style
                 lookup takes the nearest matching style in the resource tree, so this one
                 wins over the app-wide one specifically for text inside the popup, in
                 every state - unselected, highlighted, and selected alike. -->
            <Style x:Key="ComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Padding" Value="10,7"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                            <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <Border.Resources>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Style>
                                </Border.Resources>
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#DDE6F0"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#C9D8EA"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemStyle}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>

                                <Border x:Name="MainBorder" Grid.ColumnSpan="2"
                                        Background="{TemplateBinding Background}" CornerRadius="6"/>

                                <TextBox x:Name="PART_EditableTextBox"
                                         Grid.Column="0"
                                         Margin="{TemplateBinding Padding}"
                                         Background="Transparent"
                                         Foreground="{TemplateBinding Foreground}"
                                         CaretBrush="{TemplateBinding Foreground}"
                                         BorderThickness="0"
                                         Focusable="True"
                                         VerticalAlignment="Center"
                                         Visibility="Hidden"/>

                                <ContentPresenter x:Name="ContentSite"
                                                  Grid.Column="0"
                                                  Margin="{TemplateBinding Padding}"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  VerticalAlignment="Center"
                                                  IsHitTestVisible="False"/>

                                <ToggleButton x:Name="ToggleBtn"
                                              Grid.Column="1"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="Transparent">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border Background="Transparent">
                                                <Path Data="M0,0 L6,6 L12,0 Z" Fill="{StaticResource TextMutedBrush}"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>

                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Border Background="White" BorderBrush="#C7CDD6" BorderThickness="1" CornerRadius="6"
                                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                            MaxHeight="260" Margin="0,2,0,0">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.3"/>
                                        </Border.Effect>
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="2">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEditable" Value="True">
                                    <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="MainBorder" Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- === CheckBox (dark theme) ===
                 No style existed for CheckBox before, so it rendered as a plain default
                 Windows checkbox: a white square with a black checkmark and a gray 3D-ish
                 border - the one obviously unstyled control left in an otherwise fully
                 dark-themed app. Implicit style (TargetType only), applies everywhere. -->
            <Style TargetType="{x:Type CheckBox}">
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="Box" Width="18" Height="18" CornerRadius="4"
                                        Background="{StaticResource BgCardBrush}"
                                        BorderBrush="{StaticResource TextMutedBrush}" BorderThickness="1"
                                        VerticalAlignment="Center">
                                    <Path x:Name="CheckMark" Data="M2,6 L6,10 L14,1" Stroke="{StaticResource BgDeepBrush}"
                                          StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"
                                                   Content="{TemplateBinding Content}"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box" Property="Background" Value="{StaticResource AccentCyanBrush}"/>
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentCyanBrush}"/>
                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentCyanBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>

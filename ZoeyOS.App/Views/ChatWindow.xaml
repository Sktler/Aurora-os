<Window x:Class="ZoeyOS.App.Views.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aurora Chat" Width="900" Height="700" MinWidth="620" MinHeight="480"
        Background="{StaticResource BgDeepBrush}" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Chat with Aurora" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock Text="Your full conversation window" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
        </StackPanel>
        <Border Grid.Row="1" Background="#0E131D" BorderBrush="#202A3A" BorderThickness="1" CornerRadius="16" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding SelectedCompanion.Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#151B27" CornerRadius="12" Padding="12" Margin="4,4,4,8">
                                <StackPanel>
                                    <TextBlock Text="{Binding Role}" FontSize="11" Foreground="{StaticResource AccentCyanBrush}"/>
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBox Text="{Binding SelectedCompanion.DraftMessage, UpdateSourceTrigger=PropertyChanged}" MinHeight="46" Padding="12" AcceptsReturn="True" TextWrapping="Wrap" Background="#0E131D" BorderBrush="#293245" Foreground="{StaticResource TextPrimaryBrush}"/>
            <Button Grid.Column="1" Content="🎙" Command="{Binding SelectedCompanion.ListenCommand}" Margin="8,0,0,0" Padding="16,10"/>
            <Button Grid.Column="2" Content="Send" Command="{Binding SelectedCompanion.SendCommand}" Margin="8,0,0,0" Padding="18,10" Background="#4B16A0" Foreground="White" BorderThickness="0"/>
        </Grid>
    </Grid>
</Window>

<Window x:Class="ZoeyOS.App.Views.JamendoSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aurora Music - Jamendo" Width="560" Height="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#11151A" Foreground="#F3F6F8">
    <Border Padding="28" Background="#171C22" CornerRadius="14" Margin="12">
        <StackPanel>
            <TextBlock Text="🎵 Aurora Music" FontSize="24" FontWeight="Bold" Margin="0,0,0,6"/>
            <TextBlock Text="Connect Jamendo for free music search and playback." Foreground="#AAB4BE" TextWrapping="Wrap" Margin="0,0,0,22"/>

            <TextBlock Text="Jamendo Client ID" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="ClientIdBox" Grid.Column="0" Height="34" Padding="8" Background="#222A33" Foreground="#F3F6F8" BorderThickness="0"/>
                <Button Content="Get Client ID" Grid.Column="1" Click="GetClientId_Click" Padding="14,8" Margin="10,0,0,0" Background="#222A33" Foreground="#F3F6F8" BorderThickness="0"/>
            </Grid>

            <TextBlock Text="Create a Jamendo application, copy its Client ID, and paste it above. Aurora only needs the Client ID; do not enter or share the Client Secret."
                       Foreground="#8F9AA5" FontSize="11" TextWrapping="Wrap" Margin="0,8,0,18"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Skip for now" Click="Skip_Click" Padding="14,8" Margin="0,0,8,0" Background="#222A33" Foreground="#F3F6F8" BorderThickness="0"/>
                <Button Content="Save &amp; Connect" Click="Save_Click" Padding="16,8" Background="#4FD8E8" Foreground="#11151A" BorderThickness="0" FontWeight="Bold"/>
            </StackPanel>
            <TextBlock x:Name="StatusText" Foreground="#F0745C" FontSize="11" Margin="0,12,0,0"/>
        </StackPanel>
    </Border>
</Window>
